import 'package:flutter/material.dart';

class AppColors {
  // Основные цвета для MAD Shop (по дизайну)
  static const primary = Color(0xFF2196F3); // Синий из дизайна
  static const primaryDark = Color(0xFF1976D2);
  static const primaryLight = Color(0xFFBBDEFB);
  static const accent = Color(0xFFFFC107);
  static const background = Color(0xFFFFFFFF); // Белый фон
  static const backgroundLight = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  
  // Дополнительные цвета
  static const textPrimary = Color(0xFF000000);
  static const textSecondary = Color(0xFF757575);
  static const divider = Color(0xFFE0E0E0);
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);
  static const favorite = Color(0xFFE91E63); // Красный для избранного
  
  // Цвета для карточек товаров
  static const cardBackground = Color(0xFFFFFFFF);
  static const cardShadow = Color(0x1A000000);
  
  // Цвета для декоративных элементов
  static const decorativeBlue = Color(0xFFE3F2FD);
  static const decorativeBlueLight = Color(0xFFF3F9FF);
}

